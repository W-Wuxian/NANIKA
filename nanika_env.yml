name: nanika_env
channels:
 - defaults
 - pytorch
 - xformers
 - nvidia
 - conda-forge
dependencies:
 - python>=3.8
 - fire                # Python Fire is a library for creating command line interfaces (CLIs) from absolutely any Python object
 - sentencepiece       # Unsupervised text tokenizer for Neural Network-based text generation
 - pytorch-gpu>=2.0.1  # PyTorch is an optimized tensor library for deep learning using GPUs and CPUs
 - xformers>=0.022     # Hackable and optimized Transformers building blocks, supporting a composable construction
 - simple-parsing      # A small utility for simplifying and cleaning up argument parsing scripts

# see issues at https://github.com/mistralai/mistral-src/issues?q=AttributeError%3A+%27BlockDiagonalCausalMask%27+object+has+no+attribute+%27make_local_attention%27
